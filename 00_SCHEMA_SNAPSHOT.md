# Schema Snapshot — Rumah Keripik
> Auto-generated: 2026-07-30T04:06:21.427Z
> Generate with: `npm run db:schema-snapshot`

Tabel yang terdaftar di schema.ts:

- `pelangganChatbot` → tabel `pelanggan_chatbot`
- `produkKategori` → tabel `produk_kategori`
- `produk` → tabel `produk`
- `produkVarian` → tabel `produk_varian`
- `produkMedia` → tabel `produk_media`
- `warungRetail` → tabel `warung_retail`
- `customerProfile` → tabel `customer_profile`
- `customerIdentity` → tabel `customer_identity`
- `customerAddress` → tabel `customer_address`
- `webOrderSession` → tabel `web_order_session`
- `webChatMessage` → tabel `web_chat_message`
- `transaksi` → tabel `transaksi`
- `detailTransaksi` → tabel `detail_transaksi`
- `pesanChat` → tabel `pesan_chat`
- `aiKnowledgeBase` → tabel `ai_knowledge_base`
- `broadcastCampaign` → tabel `broadcast_campaign`
- `broadcastTemplate` → tabel `broadcast_template`
- `botAutoReply` → tabel `bot_auto_reply`
- `chatLog` → tabel `chat_log`
- `memoryPelanggan` → tabel `memory_pelanggan`
- `buktiPembayaran` → tabel `bukti_pembayaran`
- `paymentProof` → tabel `payment_proof`
- `paymentOcrResult` → tabel `payment_ocr_result`
- `paymentMethod` → tabel `payment_method`
- `paymentIntent` → tabel `payment_intent`
- `orderStatusHistory` → tabel `order_status_history`
- `orderDocument` → tabel `order_document`
- `failedConversation` → tabel `failed_conversation`
- `botSetting` → tabel `bot_setting`
- `botMenuItem` → tabel `bot_menu_item`
- `waitlistProduk` → tabel `waitlist_produk`
- `ratingPelanggan` → tabel `rating_pelanggan`
- `skillLibrary` → tabel `skill_library`
- `lokasiPelanggan` → tabel `lokasi_pelanggan`
- `zonaPengiriman` → tabel `zona_pengiriman`
- `orderDraft` → tabel `order_draft`
- `orderEvents` → tabel `order_events`
- `workerJob` → tabel `worker_job`
- `workerHeartbeat` → tabel `worker_heartbeat`
- `outboundMessageQueue` → tabel `outbound_message_queue`
- `geocodeCache` → tabel `geocode_cache`
- `aiResponseCache` → tabel `ai_response_cache`
- `aiLearningReview` → tabel `ai_learning_review`
- `couriers` → tabel `couriers`
- `courierSessions` → tabel `courier_sessions`
- `deliveryAssignment` → tabel `delivery_assignment`
- `deliveryRoutePoint` → tabel `delivery_route_point`
- `customerSessions` → tabel `customer_sessions`
- `chatSessions` → tabel `chat_sessions`
- `chatMessages` → tabel `chat_messages`
- `chatCarts` → tabel `chat_carts`
- `chatCartItems` → tabel `chat_cart_items`
- `customerMemoryV3` → tabel `customer_memory_v3`
- `aiRuns` → tabel `ai_runs`
- `aiToolCalls` → tabel `ai_tool_calls`
- `recommendationEvents` → tabel `recommendation_events`
- `aiLearningEvents` → tabel `ai_learning_events`
- `adminAuditLog` → tabel `admin_audit_log`
- `expoPushTokens` → tabel `expo_push_tokens`
- `loyaltyAccounts` → tabel `loyalty_accounts`
- `loyaltyLedger` → tabel `loyalty_ledger`
- `referrals` → tabel `referrals`
- `expenseCategories` → tabel `expense_categories`
- `ledgerEntries` → tabel `ledger_entries`
- `cashReconciliation` → tabel `cash_reconciliation`
- `backupRestoreDrills` → tabel `backup_restore_drills`
- `idempotencyKeys` → tabel `idempotency_keys`
- `rateLimits` → tabel `rate_limits`
- `courierEarnings` → tabel `courier_earnings`
- `trackingEvents` → tabel `tracking_events`
- `aiBudgetConfig` → tabel `ai_budget_config`
- `sosEvents` → tabel `sos_events`
- `skillDrafts` → tabel `skill_drafts`
- `deviceTokens` → tabel `device_tokens`
- `deviceIdentityLinks` → tabel `device_identity_links`
- `oauthLinks` → tabel `oauth_links`
- `otpRequests` → tabel `otp_requests`
- `shifts` → tabel `shifts`
- `deliveryEvents` → tabel `delivery_events`
- `notifications` → tabel `notifications`
- `courierPerformanceDaily` → tabel `courier_performance_daily`
- `courierLocations` → tabel `courier_locations`

Total: 82 tabel